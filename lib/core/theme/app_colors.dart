import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand — deep forest green: family roots, growth, trust, Islamic heritage
  static const Color primary = Color(0xFF2D6A4F);
  static const Color primaryLight = Color(0xFF52B788);
  static const Color primaryDark = Color(0xFF1B4332);
  static const Color primaryContainer = Color(0xFFD8F3DC);

  // Gold accent — honesty, heritage, prestige
  static const Color accent = Color(0xFFC9A84C);
  static const Color accentLight = Color(0xFFFFF3CD);

  // Backgrounds — warm ivory (purity, warmth)
  static const Color background = Color(0xFFFBF8F2);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF2F8F5);
  static const Color card = Color(0xFFFFFFFF);

  // Borders & Dividers
  static const Color border = Color(0xFFCFE3D8);
  static const Color divider = Color(0xFFE8F4EE);

  // Text
  static const Color textPrimary = Color(0xFF1A2E25);
  static const Color textSecondary = Color(0xFF4A7C59);
  static const Color textTertiary = Color(0xFF8EB49A);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);

  // Gender indicators
  static const Color male = Color(0xFF1D6FA4);
  static const Color maleLight = Color(0xFFDBEAFE);
  static const Color female = Color(0xFFB5446E);
  static const Color femaleLight = Color(0xFFFCE7F3);

  // Deceased
  static const Color deceased = Color(0xFF6B7280);
  static const Color deceasedLight = Color(0xFFF3F4F6);

  // Shadow
  static const Color shadow = Color(0x1A1A2E25);
  static const Color shadowLight = Color(0x0D1A2E25);
}
